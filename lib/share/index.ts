/**
 * Share plumbing — Web Share API + clipboard fallback, and PNG export.
 *
 * Built in stage 4 (docs/dev-prompts.md). The folder exists from stage 0 so the
 * structure matches docs/tech-stack.md §5.
 */

export {}
