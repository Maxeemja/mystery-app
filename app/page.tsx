/**
 * Placeholder for the «Мої бажання» hub.
 *
 * Stage 0 builds the foundation only — this file is replaced in stage 1 (which
 * adds the profile gate) and stage 2 (which adds the list itself).
 */
export default function HomePage() {
  return (
    <main className="mx-auto flex min-h-screen max-w-page items-center px-6">
      <p className="text-mid-gray text-body">
        Foundation ready. Screens land in the next stages.
      </p>
    </main>
  )
}
