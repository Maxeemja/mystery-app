import { WishlistScreen } from '../components/WishlistScreen'

/** Server shell; the list itself is client-side (docs/tech-stack.md §3). */
export default function HomePage() {
  return <WishlistScreen />
}
